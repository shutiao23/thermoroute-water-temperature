# SI15 — reproduction hashes, commands and parity

**Status:** DRAFT / PRE-OPENING / NOT RECEIPT.

| Binding | path/identifier | SHA-256 or version | verification command/role | status | binder row ID |
|---|---|---|---|---|---|
| source tree | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` | source identity | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` |
| protocol/model suite | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` | chronology/model authority | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` |
| panel/registry/input closure | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` | immutable inputs | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` |
| runtime/container lock | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` | environment identity | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` |
| replay/render receipts | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` | parity and publication closure | `[pending — 探索期数据，不可写入结论]` | `[pending — 探索期数据，不可写入结论]` |

The final SI must provide literal top-level commands and declared numerical
tolerances. A local successful run is not an independent clean-room replay.
Every displayed path, hash/version and status follows the README cell-level
binder contract.
