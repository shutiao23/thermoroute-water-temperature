# Pinned direct dependency versions for the formal Route-A build, opening, and
# replay evidence. The fully hashed transitive Python 3.12 lock is
# requirements-lock-py312-hashed.txt.
# No current performance numbers are authorized until the sealed Route-A
# opening completes; superseded historical result prose is non-authoritative.
# Install with:
#     pip install -r requirements-lock.txt
# Built and tested on Python 3.12 (macOS arm64). A clean reinstall should
# support conditional replay under the validated formal numerical runtime;
# see requirements.txt for the looser floors used for forward compatibility.

numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
lightgbm==4.6.0
torch==2.12.0
matplotlib==3.9.2
statsmodels==0.14.2
pyarrow==24.0.0
pytest==8.0.0
ruff==0.15.14
mypy==1.11.2
dataretrieval==1.2.0
pypandoc_binary==1.17    # canonical Markdown -> AGU TeX check; optional PDF/DOCX builds
